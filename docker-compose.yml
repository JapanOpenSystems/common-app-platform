services:
  react-app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./my-react-router-app:/usr/src/app 
    working_dir: /usr/src/app/my-react-router-app
    command: yarn dev
    ports:
      - "3000:3000"
      - "5173:5173"
    stdin_open: true 